Why write it

	Many, many projects, but I forget what the last modified


How to use

	cd /usr/bin/

	sudo ln -s ~/Workspace/check-all-project-status/git-st-all

	sudo ln -s ~/Workspace/check-all-project-status/git-st-one

	cd ~/Workspace

	git-st-all

Add to git alias
	git config --global alias.all-st "st-all"